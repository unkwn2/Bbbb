package com.sr.openbyd.utils

object AppConstants {
    // Main Display (UI 7 Split Screen)
    const val DEFAULT_MAIN_WIDTH = 1275
    const val DEFAULT_MAIN_HEIGHT = 900
    const val DEFAULT_MAIN_X_OFFSET = 0
    const val DEFAULT_MAIN_Y_OFFSET = 85
    const val DEFAULT_MAIN_X_OFFSET_RIGHT = 645

    // Display Margins / Bar Heights
    const val STATUS_BAR_HEIGHT = 85
    const val BOTTOM_NAV_BAR_HEIGHT = 95
    const val TOTAL_BAR_HEIGHTS = STATUS_BAR_HEIGHT + BOTTOM_NAV_BAR_HEIGHT

    // Fullscreen Configuration
    const val DEFAULT_FULL_WIDTH = 1920
    const val DEFAULT_FULL_HEIGHT = 720
    const val DEFAULT_FULL_X_OFFSET = 0
    const val DEFAULT_FULL_Y_OFFSET = 0

    // Mini Mode Configuration
    const val DEFAULT_MINI_WIDTH = 620
    const val DEFAULT_MINI_HEIGHT = 720
    const val DEFAULT_MINI_X_OFFSET = 1300
    const val DEFAULT_MINI_Y_OFFSET = 0
    const val DEFAULT_MINI_DPI = 250

    // Package
    const val PACKAGE_NAME = "com.sr.openbyd"
    
    // Timing
    // Delay used to check the proxy on launch, if it's not active, send a toast message, 12s to give the user time to accept ADB permissions
    const val PROXY_CHECK_DELAY_MS = 12000L
}
